# 校园二手交易系统接口约束与质量检测说明

## 1. 文档目的

本文档用于说明校园二手交易系统后端接口的约束设计、鉴权规则、业务校验规则和质量检测结果。答辩时可用于回答“接口有没有约束”“如何保证接口安全”“接口响应速度如何”“系统质量如何验证”等问题。

## 2. 接口约束设计

本系统的接口约束分为三层：

1. 参数格式约束：通过 Gin 的 `binding` 标签对请求参数进行必填、长度、范围和枚举校验。
2. 登录鉴权约束：通过 JWT Token 判断用户身份，保护需要登录后才能访问的接口。
3. 业务规则约束：在 Service 层判断用户权限、商品状态、交易状态和评价条件，防止非法流程。

这种设计保证了即使用户绕过前端页面直接调用接口，后端仍然能够拦截非法请求。

## 3. 通用响应格式

系统后端统一返回 JSON 格式：

```json
{
  "code": 200,
  "message": "操作结果说明",
  "data": {}
}
```

失败时返回示例：

```json
{
  "code": 400,
  "message": "请输入手机号",
  "data": null
}
```

常见状态码：

| 状态码 | 含义 | 场景 |
| --- | --- | --- |
| 200 | 请求成功 | 查询、创建、更新成功 |
| 400 | 请求参数或业务规则不满足 | 参数缺失、状态不允许、权限不满足 |
| 401 | 未登录或 Token 无效 | 缺少 Token、Token 过期、Token 格式错误 |
| 404 | 资源不存在 | 商品、用户、留言、评价不存在 |
| 500 | 服务端异常 | 数据库或内部服务异常 |

## 4. 登录鉴权约束

系统使用 JWT 进行接口鉴权。用户登录成功后，后端返回 Token；前端后续请求在请求头中携带：

```http
Authorization: Bearer <token>
```

未携带 Token、Token 格式错误、Token 无效或过期时，接口返回 `401`。

### 4.1 公开接口

以下接口不需要登录：

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/ping` | GET | 健康检查 |
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/login` | POST | 用户登录 |
| `/api/products` | GET | 商品列表 |
| `/api/products/:productId` | GET | 商品详情 |
| `/api/products/:productId/messages` | GET | 商品留言列表 |
| `/api/products/:productId/reviews` | GET | 商品评价列表 |
| `/api/users/:userId/reviews` | GET | 用户历史评价 |

### 4.2 需要登录的接口

以下接口必须携带有效 Token：

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/user/profile` | GET | 查看个人信息 |
| `/api/user/profile` | PUT | 修改个人信息 |
| `/api/products` | POST | 发布商品 |
| `/api/products/:productId` | PUT | 修改商品 |
| `/api/my/products` | GET | 我的商品 |
| `/api/products/:productId/off-shelf` | PUT | 下架商品 |
| `/api/products/:productId/transactions` | POST | 发起交易申请 |
| `/api/my/buy-transactions` | GET | 我发起的交易 |
| `/api/my/sell-transactions` | GET | 我收到的交易 |
| `/api/transactions/:transactionId/accept` | PUT | 接受交易 |
| `/api/transactions/:transactionId/reject` | PUT | 拒绝交易 |
| `/api/transactions/:transactionId/complete` | PUT | 完成交易 |
| `/api/products/:productId/messages` | POST | 发布留言 |
| `/api/products/:productId/messages/:messageId/replies` | POST | 回复留言 |
| `/api/messages/:messageId` | DELETE | 删除留言 |
| `/api/transactions/:transactionId/reviews` | POST | 发布评价 |
| `/api/reviews/:reviewId` | PUT | 修改评价 |

## 5. 参数约束

### 5.1 注册与登录

| 字段 | 约束 | 说明 |
| --- | --- | --- |
| `username` | 必填，3-50 字符 | 用户名 |
| `password` | 必填，6-50 字符 | 密码 |
| `nickname` | 最多 50 字符 | 昵称 |
| `phone` | 必填，11 位数字 | 手机号 |

约束意义：

- 手机号强制填写，保证交易双方可以线下联系。
- 密码最少 6 位，避免过短密码。
- 用户名唯一，避免账号冲突。

### 5.2 个人信息

| 字段 | 约束 | 说明 |
| --- | --- | --- |
| `nickname` | 最多 50 字符 | 昵称 |
| `phone` | 必填，11 位数字 | 手机号 |
| `avatar` | 最多 255 字符 | 头像地址 |

### 5.3 商品发布与修改

| 字段 | 约束 | 说明 |
| --- | --- | --- |
| `title` | 必填，最多 100 字符 | 商品标题 |
| `description` | 最多 2000 字符 | 商品描述 |
| `price` | 必填，必须大于 0 | 商品价格 |
| `category` | 最多 50 字符 | 商品分类 |
| `imageUrl` | 可选 | 商品图片 |
| `status` | 修改时必填，必须是指定枚举 | 商品状态 |

商品状态枚举：

| 状态 | 含义 |
| --- | --- |
| `available` | 可交易 |
| `trading` | 交易中 |
| `sold` | 已售出 |
| `off_shelf` | 已下架 |

### 5.4 交易申请

| 字段 | 约束 | 说明 |
| --- | --- | --- |
| `remark` | 必填，最多 500 字符 | 交易说明 |

交易说明要求用户填写面交时间、地点或补充联系方式，避免卖家无法联系买家。

### 5.5 留言与回复

| 字段 | 约束 | 说明 |
| --- | --- | --- |
| `content` | 必填，最多 500 字符 | 留言内容 |
| `imageUrl` | 可选 | 留言图片 |

### 5.6 评价

| 字段 | 约束 | 说明 |
| --- | --- | --- |
| `rating` | 必填，1-5 分 | 评分 |
| `content` | 必填，最多 500 字符 | 评价内容 |
| `imageUrl` | 可选 | 评价图片 |

## 6. 业务规则约束

### 6.1 商品权限约束

| 规则 | 说明 |
| --- | --- |
| 只能修改自己的商品 | 防止用户篡改他人商品 |
| 只能下架自己的商品 | 防止恶意下架他人商品 |
| 商品不存在时返回错误 | 防止对不存在资源操作 |

### 6.2 交易流程约束

| 规则 | 说明 |
| --- | --- |
| 不能购买自己发布的商品 | 避免无意义交易 |
| 只有 `available` 商品可以发起交易 | 防止交易中、已售出、已下架商品继续被申请 |
| 买家手机号必须存在 | 方便卖家联系买家 |
| 交易说明不能为空 | 保证交易地点、时间等信息完整 |
| 同一买家不能对同一商品重复发起待处理或已接受交易 | 防止重复申请 |
| 只有卖家可以接受交易 | 防止买家自己接受 |
| 只有卖家可以拒绝交易 | 防止其他用户操作 |
| 只有交易相关买家或卖家可以完成交易 | 防止无关用户修改状态 |
| 只有 `pending` 状态可以接受或拒绝 | 防止重复操作 |
| 只有 `accepted` 状态可以完成交易 | 保证交易流程顺序 |

交易状态流转：

```text
pending -> accepted -> completed
pending -> rejected
```

商品状态联动：

```text
available --卖家接受交易--> trading
trading --交易完成--> sold
available --卖家下架--> off_shelf
```

### 6.3 留言权限约束

| 规则 | 说明 |
| --- | --- |
| 登录后才能留言或回复 | 保证留言可追溯 |
| 留言内容不能为空 | 防止空留言 |
| 回复的父留言必须存在 | 防止无效回复 |
| 只能删除自己的留言 | 防止删除他人内容 |
| 删除留言采用软删除 | 保留结构，避免楼中楼关系异常 |

### 6.4 评价权限约束

| 规则 | 说明 |
| --- | --- |
| 只有完成的交易才能评价 | 保证评价来自真实交易 |
| 只有交易相关用户可以评价 | 防止无关用户刷评价 |
| 同一交易同一用户只能评价一次 | 防止重复评价 |
| 只能修改自己的评价 | 防止篡改他人评价 |
| 评价评分必须在 1-5 分 | 保证评分数据可统计 |

## 7. 安全性质量检测

### 7.1 鉴权测试

测试内容：

- 不带 Token 访问 `/api/user/profile`。
- 使用错误 Token 访问受保护接口。
- 使用正确 Token 访问个人信息、交易列表等接口。

预期结果：

- 未登录或 Token 错误返回 `401`。
- Token 正确时返回用户数据。

测试结论：

系统受保护接口能够正确识别登录状态，未登录用户不能访问个人数据和交易操作接口。

### 7.2 参数校验测试

测试内容：

- 注册时不填手机号。
- 注册时手机号不足 11 位。
- 发布商品时价格为 0。
- 发起交易时不填交易说明。
- 评价时评分小于 1 或大于 5。

预期结果：

- 后端返回 `400`。
- 返回中文业务提示，而不是原始字段错误。

测试结论：

参数校验覆盖了主要用户输入场景，能够防止无效数据进入数据库。

### 7.3 权限边界测试

测试内容：

- 买家尝试修改卖家的商品。
- 买家尝试接受自己的交易申请。
- 无关用户尝试完成交易。
- 用户尝试删除他人留言。
- 用户尝试修改他人评价。

预期结果：

- 返回 `400` 或权限相关错误提示。
- 数据库状态不发生非法变更。

测试结论：

系统在 Service 层做了用户身份和资源归属判断，能够保证核心数据不被越权修改。

### 7.4 状态流转测试

测试内容：

- 对已售出商品发起交易。
- 对非 `pending` 交易执行接受或拒绝。
- 对非 `accepted` 交易执行完成。
- 对未完成交易发布评价。

预期结果：

- 非法状态操作被拒绝。
- 商品状态和交易状态保持一致。

测试结论：

交易流程约束比较完整，能够避免交易状态乱跳。

## 8. 接口性能检测

### 8.1 测试环境

| 项目 | 内容 |
| --- | --- |
| 测试时间 | 2026-05-26 |
| 测试环境 | 本地开发环境 |
| 后端框架 | Go + Gin + GORM |
| 数据库 | SQLite |
| 测试地址 | `http://localhost:8080/api` |
| 采样方式 | 每个接口连续请求 15 次，统计平均值、最小值、P95 和最大值 |

说明：

- 本次结果是本地环境采样，不等同于服务器压测结果。
- 本地数据库数据量较小，因此响应时间主要用于说明接口当前实现没有明显慢查询或阻塞。
- 登录接口包含密码哈希校验，因此耗时明显高于普通查询接口，属于正常现象。

### 8.2 响应时间采样结果

| 接口 | 方法 | 样本数 | 成功数 | 失败数 | 平均响应时间 | 最小响应时间 | P95 响应时间 | 最大响应时间 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `/api/ping` | GET | 15 | 15 | 0 | 0.78 ms | 0.30 ms | 6.45 ms | 6.45 ms |
| `/api/products?page=1&pageSize=20&status=available` | GET | 15 | 15 | 0 | 1.79 ms | 1.33 ms | 3.02 ms | 3.02 ms |
| `/api/products/8` | GET | 15 | 15 | 0 | 0.81 ms | 0.69 ms | 1.32 ms | 1.32 ms |
| `/api/products/8/messages` | GET | 15 | 15 | 0 | 0.73 ms | 0.58 ms | 0.94 ms | 0.94 ms |
| `/api/products/8/reviews` | GET | 15 | 15 | 0 | 0.49 ms | 0.41 ms | 0.63 ms | 0.63 ms |
| `/api/auth/login` | POST | 15 | 15 | 0 | 49.08 ms | 48.07 ms | 50.65 ms | 50.65 ms |
| `/api/user/profile` | GET | 15 | 15 | 0 | 0.65 ms | 0.52 ms | 1.37 ms | 1.37 ms |
| `/api/my/buy-transactions` | GET | 15 | 15 | 0 | 1.80 ms | 0.97 ms | 9.19 ms | 9.19 ms |

### 8.3 性能结论

1. 查询类接口平均响应时间基本在 2 ms 内，本地测试表现良好。
2. 登录接口平均约 49 ms，主要耗时来自密码哈希校验，相比普通查询更慢是合理的。
3. 所有采样接口成功率为 100%，没有出现请求失败。
4. 商品列表、商品详情、留言、评价等核心展示接口响应稳定。
5. 当前数据量下没有发现明显慢接口。

## 9. 前端质量检测

### 9.1 构建检测

执行命令：

```bash
npm run build
```

检测结果：

- 前端构建通过。
- Vite 打包成功。
- 构建过程中存在 chunk size 提示，主要原因是 Ant Design 等依赖体积较大，不影响功能运行。

### 9.2 后端编译检测

执行命令：

```bash
go build ./...
```

检测结果：

- 后端编译通过。
- Controller、Service、Model、Request、Response 等模块均可正常编译。

### 9.3 浏览器功能检测

检测页面：

| 页面 | 检测内容 | 结果 |
| --- | --- | --- |
| 商品广场 | 商品列表、筛选区、商品卡片、页面布局 | 正常 |
| 商品详情 | 商品图片、交易流程、留言区、评价区 | 正常 |
| 交易列表 | 状态筛选、交易摘要、空状态 | 正常 |
| 登录页 | 表单输入、登录跳转、Token 写入 | 正常 |

### 9.4 UI 质量检测

检测内容：

- 页面是否空白。
- 页面是否横向溢出。
- 商品图片是否正常显示。
- 留言输入框、上传图片按钮、发送按钮是否正常。
- 交易页状态筛选是否正常显示。
- 主要标题、副标题是否与业务一致。

检测结论：

核心页面渲染正常，未发现空白页或桌面端横向溢出问题。

## 10. 数据一致性设计

系统中交易状态和商品状态存在联动：

- 卖家接受交易后，交易状态变为 `accepted`，商品状态变为 `trading`。
- 交易完成后，交易状态变为 `completed`，商品状态变为 `sold`。
- 卖家拒绝交易时，交易状态变为 `rejected`。

这些状态更新在后端统一处理，避免前端直接修改状态造成数据不一致。

## 11. 已落地优化与后续改进方向

根据质量检测结果，系统已进一步补充了以下低风险质量优化：

| 优化项 | 落地方式 | 质量收益 |
| --- | --- | --- |
| 分页上限保护 | 商品列表接口限制 `pageSize` 最大为 `50` | 防止用户一次请求过多数据，降低数据库和接口压力 |
| 路径参数校验 | 对 `productId`、`transactionId`、`messageId`、`reviewId`、`userId` 做正整数校验 | 避免非法路径参数被转换为 `0` 后继续查库 |
| 接口耗时监控 | 新增请求耗时中间件，日志记录方法、路径、状态码、耗时 | 方便定位慢接口，答辩时可说明有基础性能观测能力 |
| 响应时间响应头 | 接口返回 `X-Response-Time` 响应头 | 前端或测试工具可以直接看到单次请求耗时 |
| 图片字段大小约束 | 商品图、留言图、评价图字段限制最大长度 | 防止超大 data URL 导致请求体和数据库字段异常膨胀 |

当前系统已经具备基本接口约束和质量保障，但仍有可继续优化的方向：

| 方向 | 当前情况 | 后续优化 |
| --- | --- | --- |
| 接口压测 | 已做本地响应时间采样 | 可使用 JMeter、wrk 或 k6 做并发压测 |
| 分页保护 | 已限制最大 `pageSize` 为 `50` | 后续可对更多列表接口统一接入分页 |
| 图片存储 | 已限制图片字段大小，当前仍使用 data URL 或图片地址 | 后续可接入对象存储，提高大图性能 |
| 日志监控 | 已增加接口耗时日志和 `X-Response-Time` | 后续可接入 Prometheus、Grafana 或日志平台 |
| 权限模型 | 当前普通用户角色足够 | 后续可增加管理员角色和审核机制 |
| 数据库 | 当前使用 SQLite | 部署时可切换 MySQL，提高并发能力 |

## 12. 答辩可用总结

可以这样回答老师：

> 我们系统的接口约束主要分为三层：第一层是参数约束，比如手机号必须 11 位、价格必须大于 0、交易说明必填、评分必须是 1 到 5 分；第二层是登录鉴权约束，发布商品、发起交易、留言、评价等接口都必须携带 JWT Token；第三层是业务规则约束，比如不能购买自己的商品，只有卖家能接受或拒绝交易，只有完成的交易才能评价，用户只能删除自己的留言和修改自己的评价。
>
> 质量检测方面，我们做了前端构建、后端编译、接口响应时间采样和浏览器页面检查。当前本地环境下，大部分查询接口平均响应时间在 2 ms 以内，登录接口因为包含密码哈希校验，平均约 49 ms。核心接口采样成功率是 100%，主要页面也没有出现空白或横向溢出。

## 13. 优化后复测记录

### 13.1 复测环境

| 项目 | 内容 |
| --- | --- |
| 复测时间 | 2026-05-26 |
| 测试环境 | 本地开发环境 |
| 后端测试端口 | `http://localhost:18080/api` |
| 数据库 | SQLite |
| 说明 | 使用临时端口启动当前后端代码，避免影响默认 `8080` 服务 |

### 13.2 接口约束复测结果

| 测试项 | 测试方法 | 预期结果 | 实际结果 | 结论 |
| --- | --- | --- | --- | --- |
| 健康检查响应头 | 请求 `/api/ping` | 返回 `200`，包含 `X-Response-Time` | `HTTP 200`，包含 `X-Response-Time=0.00ms` | 通过 |
| 未登录访问个人信息 | 不带 Token 请求 `/api/user/profile` | 返回 `401` | `HTTP 401` | 通过 |
| Token 登录访问个人信息 | 携带有效 Token 请求 `/api/user/profile` | 返回 `200` | `HTTP 200` | 通过 |
| 非法商品 ID 参数 | 请求 `/api/products/abc` | 返回 `400` | `HTTP 400` | 通过 |
| 分页上限保护 | 请求 `/api/products?pageSize=999` | `pageSize` 被限制为 `50` | 返回 `pageSize=50` | 通过 |
| 注册手机号必填 | 注册请求不传 `phone` | 返回 `400` | `HTTP 400` | 通过 |
| 交易说明必填 | 发起交易时 `remark` 为空 | 返回 `400` | `HTTP 400` | 通过 |
| 超大图片字段限制 | 发布商品时传入超长 `imageUrl` | 返回 `400` | `HTTP 400` | 通过 |

### 13.3 构建与编译复测

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 后端编译 | `go build ./...` | 通过 |
| 前端构建 | `npm run build` | 通过 |

前端构建仍存在 Vite 的 chunk size 提示，原因是 Ant Design 等依赖体积较大；该提示不影响本次功能运行和接口质量优化验证。

### 13.4 复测结论

本轮复测覆盖了接口鉴权、Token 正常访问、非法路径参数、分页上限、必填字段、图片字段大小约束和响应时间可观测能力。复测结果全部通过，说明新增的接口质量保护已经生效。
