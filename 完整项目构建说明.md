# 校园二手商品交易系统完整项目构建说明

> 用途：把这份 Markdown 直接交给 Codex / Claude Code / 其他代码生成工具，让它按照本文档生成一个可运行的「校园二手商品交易系统」完整项目。
>
> 项目定位：软件工程课程作业 / CodeArts Scrum 团队协作项目。
>
> 开发方式：前后端分离，后端提供 RESTful API，前端调用接口完成页面交互。
>
> 项目范围：完整实现用户管理、商品管理、交易管理、留言评价四个子系统。

---

# 一、项目背景

校园中存在大量闲置物品，例如教材、电子产品、生活用品、学习资料等。传统二手交易方式通常依赖微信群、QQ群或线下沟通，存在信息分散、交易状态不清晰、买卖双方沟通不方便、交易记录难以追踪等问题。

本项目拟开发一个面向校园内部用户的二手商品交易系统，用于规范校园二手商品发布、浏览、交易申请、交易确认、留言沟通和交易评价等流程。系统通过统一平台集中展示商品信息，使用户可以发布闲置商品、浏览商品详情、发起交易申请、处理交易请求、发表留言、回复留言并在交易完成后进行评价。

---

# 二、项目总体目标

需要完成一个最小可运行的校园二手商品交易系统，至少包含以下功能：

1. 用户注册、登录、退出登录。
2. 用户查看和修改个人信息。
3. 用户发布二手商品。
4. 用户修改自己发布的商品。
5. 用户查看商品列表和商品详情。
6. 用户查看自己发布的商品。
7. 买家对商品发起交易申请。
8. 卖家接受或拒绝交易申请。
9. 买卖双方完成交易。
10. 用户查看自己的交易记录。
11. 用户在商品详情页发表留言。
12. 用户回复商品留言。
13. 用户查看商品留言。
14. 用户删除自己发布的留言。
15. 交易完成后，交易相关用户发布评价。
16. 用户查看商品评价。
17. 用户修改自己发布的评价。
18. 系统进行基础权限校验，防止非本人修改、删除或操作数据。

---

# 三、技术栈要求

## 3.1 后端

- 语言：Go
- Web 框架：Gin
- ORM：GORM
- 数据库：MySQL
- 鉴权方式：JWT Token
- 密码处理：bcrypt
- 跨域处理：CORS
- 接口风格：RESTful API
- 配置方式：config.yaml 或 .env
- 响应格式：统一 JSON 响应

## 3.2 前端

- 框架：React
- 构建工具：Vite
- UI 组件库：Ant Design
- 请求库：axios
- 路由：react-router-dom
- 登录状态：localStorage 保存 token 和用户信息
- 页面风格：简洁清晰，适合作业展示和录屏演示

---

# 四、系统角色

## 4.1 游客

未登录用户可以：

- 浏览商品列表
- 查看商品详情
- 查看商品留言
- 查看商品评价
- 注册账号
- 登录系统

## 4.2 普通用户

登录用户可以：

- 修改个人信息
- 发布商品
- 修改自己发布的商品
- 查看自己发布的商品
- 浏览商品
- 申请交易
- 处理自己商品的交易申请
- 查询自己的交易记录
- 发布留言
- 回复留言
- 删除自己的留言
- 在交易完成后发布评价
- 查看评价
- 修改自己的评价

---

# 五、系统模块划分

系统包含四个核心子系统：

1. 用户管理子系统
2. 商品管理子系统
3. 交易管理子系统
4. 留言评价子系统

---

# 六、Scrum 需求拆分

## 6.1 Epic：校园二手商品交易系统

建设一个面向校园内部用户的二手商品交易平台，实现用户注册登录、商品发布与浏览、交易申请与确认、留言沟通和交易评价等核心功能，规范校园二手商品交易流程，提高闲置物品流转效率。

## 6.2 Feature 1：用户管理子系统

负责人建议：刘东胜

### Story 1：用户注册

作为未注册用户，我希望能够通过账号信息完成注册，以便成为系统用户并使用校园二手商品交易功能。

Task：

- 设计用户注册页面
- 实现用户注册接口
- 完成用户名、密码等字段校验
- 实现密码加密存储
- 测试用户注册成功和失败场景

### Story 2：用户登录

作为已注册用户，我希望能够登录系统，以便进入个人账号并进行商品发布、交易申请和留言评价等操作。

Task：

- 设计用户登录页面
- 实现用户登录接口
- 校验账号和密码是否正确
- 登录成功后返回 JWT Token
- 前端保存 Token 和用户信息
- 测试正常登录、密码错误和用户不存在等场景

### Story 3：用户信息管理

作为系统用户，我希望能够查看和修改个人信息，以便保证交易联系信息和个人资料的准确性。

Task：

- 设计个人信息页面
- 实现用户信息查询接口
- 实现用户信息修改接口
- 完成用户信息字段校验
- 测试用户信息查看和修改功能

### Story 4：登录状态维护

作为已登录用户，我希望系统能够记住我的登录状态，以便刷新页面后仍然可以继续使用系统。

Task：

- 前端封装 Token 存取逻辑
- axios 请求自动携带 Authorization 请求头
- 后端实现 JWT 鉴权中间件
- Token 无效时前端跳转登录页
- 测试登录状态保持和失效场景

## 6.3 Feature 2：商品管理子系统

负责人建议：游晨怡

### Story 1：创建商品

作为卖家用户，我希望能够创建商品信息，以便将自己的闲置物品发布到校园二手商品交易平台。

Task：

- 设计商品发布页面
- 实现商品创建接口
- 完成商品名称、价格、分类、描述等字段校验
- 支持商品图片地址或商品描述信息录入
- 测试商品发布成功和发布失败场景

### Story 2：修改商品

作为卖家用户，我希望能够修改已发布的商品信息，以便及时更新商品价格、描述或交易状态。

Task：

- 设计商品编辑页面
- 实现商品信息查询接口
- 实现商品修改接口
- 限制只有商品发布者可以修改商品
- 测试商品信息修改和权限校验功能

### Story 3：浏览商品

作为普通用户，我希望能够浏览商品列表和商品详情，以便查找自己感兴趣的二手商品。

Task：

- 设计商品列表页面
- 设计商品详情页面
- 实现商品列表查询接口
- 实现商品详情查询接口
- 支持按分类、关键词或状态筛选商品
- 测试商品列表展示和商品详情展示功能

### Story 4：商品状态管理

作为商品发布者，我希望系统能够根据交易情况更新商品状态，以便其他用户了解商品是否仍可交易。

Task：

- 设计商品状态字段
- 实现商品上架、下架、交易中、已完成状态
- 交易申请被接受后更新商品状态
- 商品交易完成后更新商品状态
- 测试商品状态流转功能

### Story 5：我的商品

作为卖家用户，我希望能够查看自己发布的商品列表，以便统一管理我的闲置商品。

Task：

- 设计我的商品页面
- 实现我的商品查询接口
- 支持查看商品状态和发布时间
- 支持从我的商品页面进入编辑页面
- 测试我的商品列表展示功能

## 6.4 Feature 3：交易管理子系统

负责人建议：魏颖超

### Story 1：申请交易

作为买家用户，我希望能够对感兴趣的商品发起交易申请，以便向卖家表达购买意向。

Task：

- 设计商品详情页交易申请入口
- 实现交易申请接口
- 校验商品是否可交易
- 校验用户不能申请购买自己的商品
- 生成交易申请记录
- 测试正常申请、重复申请和异常申请场景

### Story 2：接受交易

作为卖家用户，我希望能够接受买家的交易申请，以便确认交易对象并推进交易流程。

Task：

- 设计交易申请处理页面
- 实现交易申请查询接口
- 实现接受交易申请接口
- 更新交易状态
- 更新商品状态
- 测试卖家接受交易和权限校验功能

### Story 3：拒绝交易

作为卖家用户，我希望能够拒绝不合适的交易申请，以便继续等待其他买家。

Task：

- 在交易申请处理页面增加拒绝按钮
- 实现拒绝交易申请接口
- 更新交易状态为 rejected
- 保持商品仍可交易
- 测试拒绝交易申请功能

### Story 4：完成交易

作为交易双方用户，我希望能够将已接受的交易标记为完成，以便后续进行交易评价。

Task：

- 设计完成交易按钮
- 实现完成交易接口
- 校验只有交易相关用户可以操作
- 更新交易状态为 completed
- 更新商品状态为 sold
- 测试完成交易状态流转

### Story 5：查询交易

作为系统用户，我希望能够查询自己的交易记录，以便了解已申请、已接受或已完成的交易情况。

Task：

- 设计交易记录页面
- 实现买家交易记录查询接口
- 实现卖家交易记录查询接口
- 展示交易状态和商品信息
- 测试不同用户的交易记录查询结果

## 6.5 Feature 4：留言评价子系统

负责人建议：肖浩阳

### Story 1：发表留言

作为用户，我希望能够在商品详情页发表留言，以便向卖家咨询商品的具体情况。

Task：

- 设计商品留言区域
- 实现留言发布接口
- 校验留言内容不能为空
- 将留言内容与商品和用户关联保存
- 测试留言发布和留言展示功能

### Story 2：回复留言

作为商品发布者或普通用户，我希望能够回复留言，以便围绕商品问题继续沟通。

Task：

- 设计留言回复功能
- 实现留言回复接口
- 支持父留言和子回复关联
- 展示留言与回复的对应关系
- 测试留言回复功能

### Story 3：查看商品留言

作为用户，我希望能够在商品详情页查看其他用户的留言内容，以便了解该商品的咨询情况和卖家的回复信息。

Task：

- 设计商品留言列表展示区域
- 实现商品留言查询接口
- 查询时过滤已删除留言
- 支持按时间排序展示
- 测试商品留言展示功能

### Story 4：删除自己的留言

作为留言发布者，我希望能够删除自己发布的留言，以便在留言内容错误或不再需要时进行处理。

Task：

- 设计留言删除操作入口
- 实现留言删除接口
- 校验用户只能删除自己的留言
- 使用逻辑删除方式处理留言
- 测试留言删除和权限控制功能

### Story 5：发布交易评价

作为交易用户，我希望能够在交易完成后发布评价，以便反馈本次交易体验。

Task：

- 设计交易评价页面
- 实现交易评价发布接口
- 校验只有交易相关用户可以评价
- 校验交易状态必须为 completed
- 保存评分和评价内容
- 测试评价发布功能

### Story 6：查看交易评价

作为用户，我希望能够查看商品或交易相关的评价信息，以便了解卖家、买家或商品交易体验。

Task：

- 设计交易评价展示区域
- 实现交易评价查询接口
- 在商品详情页展示评价列表
- 展示评分、评价内容、评价人和评价时间
- 测试交易评价展示功能

### Story 7：修改交易评价

作为交易用户，我希望能够在评价发布后修改评价内容，以便修正评分或补充交易体验说明。

Task：

- 设计评价编辑入口
- 实现交易评价修改接口
- 校验只有评价发布者可以修改评价
- 校验评分范围和评价内容
- 测试评价修改和权限校验功能

### Story 8：评价权限校验

作为系统用户，我希望只有参与交易的用户才能发布交易评价，以便保证评价内容真实有效，避免无关用户随意评价。

Task：

- 梳理交易评价权限规则
- 实现评价发布前的交易关系校验
- 实现同一用户同一交易只能评价一次
- 测试非交易用户无法评价的场景
- 测试重复评价限制场景

---

# 七、迭代规划建议

## 迭代一：基础框架与用户管理

目标：完成项目初始化、数据库连接、用户注册、用户登录、用户信息管理和 JWT 鉴权。

包含 Story：

- 用户注册
- 用户登录
- 用户信息管理
- 登录状态维护

负责人建议：刘东胜

## 迭代二：商品管理功能开发

目标：完成商品发布、商品修改、商品浏览、商品状态管理和我的商品功能。

包含 Story：

- 创建商品
- 修改商品
- 浏览商品
- 商品状态管理
- 我的商品

负责人建议：游晨怡

## 迭代三：交易管理功能开发

目标：完成交易申请、接受交易、拒绝交易、完成交易和交易记录查询。

包含 Story：

- 申请交易
- 接受交易
- 拒绝交易
- 完成交易
- 查询交易

负责人建议：魏颖超

## 迭代四：留言评价与系统完善

目标：完成留言发布、留言回复、留言查看、留言删除、评价发布、评价查看、评价修改和权限校验，并进行系统联调、页面优化和文档整理。

包含 Story：

- 发表留言
- 回复留言
- 查看商品留言
- 删除自己的留言
- 发布交易评价
- 查看交易评价
- 修改交易评价
- 评价权限校验

负责人建议：肖浩阳

---

# 八、数据库设计

## 8.1 users 用户表

```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(255) NOT NULL COMMENT '加密后的密码',
    nickname VARCHAR(50) COMMENT '昵称',
    phone VARCHAR(20) COMMENT '手机号',
    qq VARCHAR(30) COMMENT 'QQ号',
    wechat VARCHAR(80) COMMENT '微信号',
    avatar LONGTEXT COMMENT '头像地址或压缩后的 data URL',
    role VARCHAR(20) DEFAULT 'user' COMMENT '角色：user/admin',
    created_at DATETIME,
    updated_at DATETIME
);
```

## 8.2 products 商品表

```sql
CREATE TABLE products (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '发布者ID',
    title VARCHAR(100) NOT NULL COMMENT '商品标题',
    description TEXT COMMENT '商品描述',
    price DECIMAL(10, 2) NOT NULL COMMENT '商品价格',
    category VARCHAR(50) COMMENT '商品分类',
    image_url LONGTEXT COMMENT '商品图片地址或压缩后的 data URL',
    status VARCHAR(20) DEFAULT 'available' COMMENT '商品状态',
    created_at DATETIME,
    updated_at DATETIME
);
```

商品状态：

```text
available：可交易
off_shelf：已下架
trading：交易中
sold：已售出
```

## 8.3 transactions 交易表

```sql
CREATE TABLE transactions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    product_id BIGINT NOT NULL COMMENT '商品ID',
    buyer_id BIGINT NOT NULL COMMENT '买家ID',
    seller_id BIGINT NOT NULL COMMENT '卖家ID',
    status VARCHAR(20) DEFAULT 'pending' COMMENT '交易状态',
    remark TEXT COMMENT '交易申请备注',
    created_at DATETIME,
    updated_at DATETIME
);
```

交易状态：

```text
pending：待处理
accepted：已接受
rejected：已拒绝
completed：已完成
cancelled：已取消
```

## 8.4 messages 留言表

```sql
CREATE TABLE messages (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    product_id BIGINT NOT NULL COMMENT '商品ID',
    user_id BIGINT NOT NULL COMMENT '留言用户ID',
    parent_id BIGINT DEFAULT NULL COMMENT '父留言ID，用于回复',
    content TEXT NOT NULL COMMENT '留言内容',
    image_url LONGTEXT COMMENT '留言图片地址或压缩后的 data URL',
    is_deleted TINYINT DEFAULT 0 COMMENT '是否删除：0未删除，1已删除',
    created_at DATETIME,
    updated_at DATETIME
);
```

## 8.5 reviews 评价表

```sql
CREATE TABLE reviews (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    transaction_id BIGINT NOT NULL COMMENT '交易ID',
    product_id BIGINT NOT NULL COMMENT '商品ID',
    reviewer_id BIGINT NOT NULL COMMENT '评价人ID',
    target_user_id BIGINT NOT NULL COMMENT '被评价人ID',
    rating INT NOT NULL COMMENT '评分：1-5',
    content TEXT COMMENT '评价内容',
    image_url LONGTEXT COMMENT '评价图片地址或压缩后的 data URL',
    created_at DATETIME,
    updated_at DATETIME
);
```

## 8.6 notifications 消息提醒表

```sql
CREATE TABLE notifications (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '接收提醒的用户ID',
    transaction_id BIGINT COMMENT '关联交易ID',
    product_id BIGINT COMMENT '关联商品ID',
    type VARCHAR(50) NOT NULL COMMENT '提醒类型',
    title VARCHAR(120) NOT NULL COMMENT '提醒标题',
    content TEXT COMMENT '提醒内容',
    target_path VARCHAR(120) COMMENT '前端跳转路径',
    is_read TINYINT DEFAULT 0 COMMENT '是否已读：0未读，1已读',
    created_at DATETIME,
    updated_at DATETIME
);
```

提醒类型：

```text
transaction_created：新交易申请
transaction_accepted：交易申请已接受
transaction_rejected：交易申请已拒绝
transaction_completed：交易已完成
```

---

# 九、后端接口设计

所有接口统一前缀：

```text
/api
```

需要登录的接口在 Header 中携带：

```http
Authorization: Bearer <token>
```

## 9.1 用户管理接口

```http
POST /api/auth/register
POST /api/auth/login
GET  /api/user/profile
PUT  /api/user/profile
```

注册请求：

```json
{
  "username": "test01",
  "password": "123456",
  "nickname": "测试用户",
  "phone": "13800000000"
}
```

登录请求：

```json
{
  "username": "test01",
  "password": "123456"
}
```

登录响应：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "jwt-token",
    "user": {
      "id": 1,
      "username": "test01",
      "nickname": "测试用户",
      "phone": "13800000000",
      "qq": "123456",
      "wechat": "test_wechat"
    }
  }
}
```

修改个人信息请求：

```json
{
  "nickname": "测试用户",
  "phone": "13800000000",
  "qq": "123456",
  "wechat": "test_wechat",
  "avatar": "https://example.com/avatar.png"
}
```

## 9.2 商品管理接口

```http
POST /api/products
PUT  /api/products/:productId
GET  /api/products?keyword=教材&category=教材&status=available&page=1&pageSize=10
GET  /api/products/:productId
GET  /api/my/products
PUT  /api/products/:productId/off-shelf
```

创建商品请求：

```json
{
  "title": "二手教材",
  "description": "九成新，适合软件工程课程使用",
  "price": 20.5,
  "category": "教材",
  "imageUrl": "https://example.com/book.png"
}
```

## 9.3 交易管理接口

```http
POST /api/products/:productId/transactions
GET  /api/my/buy-transactions
GET  /api/my/sell-transactions
PUT  /api/transactions/:transactionId/accept
PUT  /api/transactions/:transactionId/reject
PUT  /api/transactions/:transactionId/complete
```

交易申请请求：

```json
{
  "remark": "我想购买这个商品，可以今天下午交易。"
}
```

交易记录返回中会附带评价状态：

```json
{
  "id": 1,
  "status": "completed",
  "buyer": {
    "id": 2,
    "nickname": "买家同学",
    "phone": "13800000000",
    "qq": "123456",
    "wechat": "buyer_wechat",
    "ratingAvg": 4.8,
    "ratingCount": 6
  },
  "seller": {
    "id": 3,
    "nickname": "卖家同学",
    "phone": "13900000000",
    "qq": "654321",
    "wechat": "seller_wechat",
    "ratingAvg": 4.6,
    "ratingCount": 9
  },
  "myReview": {
    "id": 10,
    "rating": 5,
    "content": "我已经评价过对方"
  },
  "peerReview": {
    "id": 11,
    "rating": 4,
    "content": "对方也评价过我"
  }
}
```

说明：交易记录中的买家、卖家对象会带上 `ratingAvg` 和 `ratingCount`，前端可以在处理交易时展示对方历史信誉。卖家处理收到的交易时能看到买家历史评分，买家查看自己发起的交易时也能看到卖家历史评分。

## 9.4 留言接口

```http
POST   /api/products/:productId/messages
POST   /api/products/:productId/messages/:messageId/replies
GET    /api/products/:productId/messages
DELETE /api/messages/:messageId
```

发布留言请求：

```json
{
  "content": "这个商品还在吗？"
}
```

回复留言请求：

```json
{
  "content": "还在，可以私聊。"
}
```

## 9.5 评价接口

```http
POST /api/transactions/:transactionId/reviews
GET  /api/products/:productId/reviews
GET  /api/users/:userId/reviews
PUT  /api/reviews/:reviewId
```

发布评价请求：

```json
{
  "rating": 5,
  "content": "交易很顺利，卖家态度很好。"
}
```

修改评价请求：

```json
{
  "rating": 4,
  "content": "整体不错，商品描述基本符合。"
}
```

## 9.6 消息提醒接口

```http
GET /api/notifications
GET /api/notifications/unread-count
PUT /api/notifications/:notificationId/read
PUT /api/notifications/read-all
```

通知列表响应：

```json
{
  "list": [
    {
      "id": 1,
      "transactionId": 3,
      "productId": 8,
      "type": "transaction_created",
      "title": "你有新的交易申请",
      "content": "买家同学 想购买「二手教材」，请及时查看并处理。",
      "targetPath": "/my/sell-transactions",
      "isRead": false,
      "createdAt": "2026-05-26T20:00:00+08:00"
    }
  ],
  "unreadCount": 1
}
```

说明：消息提醒只允许登录用户查看和修改自己的通知。新交易申请会提醒卖家；交易被接受、拒绝或完成时会提醒交易对方。

---

# 十、统一响应格式

成功响应：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

失败响应：

```json
{
  "code": 400,
  "message": "错误原因",
  "data": null
}
```

状态码建议：

```text
200：请求成功
400：参数错误
401：未登录或 Token 无效
403：无权限
404：资源不存在
500：服务器内部错误
```

---

# 十一、后端项目结构建议

```text
server/
├── main.go
├── go.mod
├── config/
│   └── config.yaml
├── database/
│   └── db.go
├── model/
│   ├── user.go
│   ├── product.go
│   ├── transaction.go
│   ├── message.go
│   ├── review.go
│   └── notification.go
├── router/
│   └── router.go
├── middleware/
│   ├── auth.go
│   └── cors.go
├── controller/
│   ├── auth_controller.go
│   ├── user_controller.go
│   ├── product_controller.go
│   ├── transaction_controller.go
│   ├── message_controller.go
│   ├── review_controller.go
│   └── notification_controller.go
├── service/
│   ├── auth_service.go
│   ├── user_service.go
│   ├── product_service.go
│   ├── transaction_service.go
│   ├── message_service.go
│   ├── review_service.go
│   └── notification_service.go
├── request/
│   ├── auth_request.go
│   ├── product_request.go
│   ├── transaction_request.go
│   ├── message_request.go
│   └── review_request.go
├── response/
│   └── response.go
└── utils/
    ├── jwt.go
    └── password.go
```

---

# 十二、前端页面设计

至少实现以下页面：

```text
/login                    登录页
/register                 注册页
/                         首页或商品列表页
/products                 商品列表页
/products/:id             商品详情页
/products/create          发布商品页
/products/:id/edit        编辑商品页
/my/products              我的商品页
/my/buy-transactions      我发起的交易页
/my/sell-transactions     我收到的交易申请页
/transactions/:id/review  发布评价页
/profile                  个人信息页
```

## 页面功能说明

### 登录页

- 输入用户名和密码
- 调用登录接口
- 登录成功后保存 token
- 跳转商品列表页

### 注册页

- 输入用户名、密码、昵称、手机号
- 调用注册接口
- 注册成功后跳转登录页

### 商品列表页

- 展示商品列表
- 支持关键词搜索
- 支持分类筛选
- 点击商品进入详情页

### 商品详情页

- 展示商品标题、描述、价格、分类、发布者、状态
- 登录用户可以发起交易申请
- 展示商品留言
- 支持发表留言
- 支持回复留言
- 支持删除自己的留言
- 展示商品评价
- 留言发布、回复、删除后只刷新留言区，避免整页刷新导致回到顶部

### 顶部消息提醒

- 登录后顶部显示消息提醒按钮和未读角标
- 新交易申请、交易接受、交易拒绝、交易完成会生成站内提醒
- 打开提醒面板可以查看最近消息、全部标记已读
- 点击单条提醒会跳转到对应交易页面

### 发布商品页

- 填写商品标题、描述、价格、分类、图片地址
- 调用商品创建接口
- 发布成功后跳转我的商品页或商品详情页

### 编辑商品页

- 回显商品信息
- 修改商品标题、描述、价格、分类、图片地址、状态
- 只能编辑自己的商品

### 我的商品页

- 展示当前用户发布的商品
- 可以进入编辑页面
- 可以下架商品

### 我发起的交易页

- 展示当前用户作为买家的交易记录
- 展示交易状态
- 展示卖家手机号、QQ、微信等联系方式
- 展示卖家历史评分和评价数量，辅助买家判断交易信誉
- 已完成交易可以进入评价页面
- 已评价交易显示“已评价”，并支持修改自己的评价

### 我收到的交易申请页

- 展示当前用户作为卖家的交易申请
- 展示买家手机号、QQ、微信等联系方式
- 展示买家历史评分和评价数量，辅助卖家处理交易申请
- pending 状态可以接受或拒绝
- accepted 状态可以完成交易
- completed 状态可以评价买家，并显示买家是否已评价卖家

### 发布评价页

- 展示交易和商品信息
- 选择评分
- 输入评价内容
- 未评价时提交评价，已评价时自动回显并支持修改

### 个人信息页

- 展示当前用户信息
- 支持修改昵称、手机号、QQ、微信、头像地址或上传本地头像

---

# 十三、前端项目结构建议

```text
client/
├── package.json
├── index.html
├── vite.config.js
├── src/
│   ├── api/
│   │   ├── auth.js
│   │   ├── user.js
│   │   ├── product.js
│   │   ├── transaction.js
│   │   ├── message.js
│   │   └── review.js
│   ├── pages/
│   │   ├── Login.jsx
│   │   ├── Register.jsx
│   │   ├── ProductList.jsx
│   │   ├── ProductDetail.jsx
│   │   ├── ProductCreate.jsx
│   │   ├── ProductEdit.jsx
│   │   ├── MyProducts.jsx
│   │   ├── BuyTransactions.jsx
│   │   ├── SellTransactions.jsx
│   │   ├── ReviewPage.jsx
│   │   └── Profile.jsx
│   ├── components/
│   │   ├── AppLayout.jsx
│   │   ├── ProductCard.jsx
│   │   ├── MessageList.jsx
│   │   ├── MessageInput.jsx
│   │   ├── ReviewList.jsx
│   │   └── ProtectedRoute.jsx
│   ├── router/
│   │   └── index.jsx
│   ├── utils/
│   │   ├── request.js
│   │   └── auth.js
│   ├── App.jsx
│   └── main.jsx
```

---

# 十四、核心业务规则

## 14.1 用户规则

1. 用户名不能重复。
2. 密码不能明文保存，必须加密。
3. 登录成功后返回 JWT Token。
4. 需要登录的接口必须校验 Token。
5. 用户只能修改自己的个人信息。

## 14.2 商品规则

1. 未登录用户不能发布商品。
2. 商品标题不能为空。
3. 商品价格必须大于 0。
4. 用户只能修改自己发布的商品。
5. 用户只能下架自己发布的商品。
6. 商品状态为 sold 时，不能继续发起交易申请。

## 14.3 交易规则

1. 未登录用户不能申请交易。
2. 用户不能购买自己发布的商品。
3. 商品状态必须为 available 才能申请交易。
4. 卖家才能接受或拒绝交易申请。
5. 交易被接受后，商品状态变为 trading。
6. 交易完成后，交易状态变为 completed，商品状态变为 sold。
7. 只有交易相关用户才能完成交易。

## 14.4 留言规则

1. 未登录用户不能发布留言。
2. 留言内容不能为空。
3. 留言内容不能超过 500 字。
4. 用户只能删除自己发布的留言。
5. 删除留言使用逻辑删除。
6. 商品不存在时不能留言。
7. 回复留言必须关联已有留言。
8. 前端发布、回复或删除留言后只刷新留言区域，不重新加载整个商品详情页，避免页面滚动位置回到顶部。

## 14.5 评价规则

1. 未登录用户不能发布评价。
2. 只有交易相关用户可以评价。
3. 交易状态必须为 completed 才能评价。
4. 同一用户对同一交易只能评价一次。
5. 评分范围为 1 到 5。
6. 评价内容不能为空或不能超过 500 字。
7. 用户只能修改自己发布的评价。
8. 买家和卖家可以分别评价对方，同一笔交易最多产生两条评价记录。
9. 交易记录中展示当前用户是否已评价，以及对方是否已评价。
10. 交易记录中展示对方历史评分和评价数量，用于体现双方信誉。

---

# 十五、运行与构建方式

## 15.1 环境要求

本项目采用前后端分离结构，首次运行前需要确认本机环境：

| 环境 | 建议版本 | 说明 |
| --- | --- | --- |
| Go | 与 `server/go.mod` 一致，当前为 `1.26.0` | 用于后端 Gin 服务编译与运行 |
| Node.js | `20.19.0+` 或 `22.12.0+` | 当前 Vite 7 对 Node 版本有要求，版本过低会导致前端启动失败 |
| npm | 随 Node.js 安装 | 用于安装前端依赖 |
| 数据库 | 默认 SQLite，可切换 MySQL | 默认使用 `server/campus_second_hand.db`，便于课程演示 |

## 15.2 后端依赖整理与启动

后端目录为 `server`，首次运行或依赖变更后建议先整理依赖：

```bash
cd server
go mod tidy
go run main.go
```

默认后端地址：

```text
http://localhost:8080
```

健康检查接口：

```bash
curl http://localhost:8080/api/ping
```

说明：

1. `go mod tidy` 会根据实际 import 自动整理 `go.mod` 和 `go.sum`。
2. 如果代码直接 import 某个库，例如 `github.com/go-playground/validator/v10`，该库应出现在直接依赖区，不应标记为 `// indirect`。
3. 后端启动成功后，控制台会输出类似 `server started at http://localhost:8080` 的日志。

## 15.3 前端依赖安装与启动

前端目录为 `client`，首次运行需要安装依赖：

```bash
cd client
npm install
npm run dev
```

默认前端地址：

```text
http://localhost:5173
```

说明：

1. 如果提示 `'vite' 不是内部或外部命令`，通常是因为还没有执行 `npm install`，或 `node_modules` 被删除后没有重新安装。
2. 如果团队需要严格按 `package-lock.json` 安装依赖，也可以使用 `npm ci`。
3. 前端开发服务器启动后，需要同时保持后端 `8080` 端口处于运行状态，页面才能正常请求接口数据。

## 15.4 完整项目构建检查

正式提交、答辩演示或换电脑运行前，建议执行完整构建检查：

```bash
cd server
go mod tidy
go build ./...
```

```bash
cd client
npm install
npm run build
```

构建结果说明：

1. `go build ./...` 通过，表示后端所有包可以正常编译。
2. `npm run build` 通过，表示前端页面和组件可以完成生产环境打包。
3. 前端构建产物位于 `client/dist/`，该目录属于自动生成文件，不需要提交到 Git。
4. `node_modules/` 也是依赖安装目录，不需要提交到 Git。

## 15.5 端口占用处理

如果启动后端时出现：

```text
listen tcp :8080: bind: Only one usage of each socket address is normally permitted
```

说明 `8080` 端口已被其他进程占用。Windows PowerShell 可按下面方式处理：

```powershell
netstat -ano | findstr :8080
Stop-Process -Id <PID> -Force
```

如果前端 `http://localhost:5173/` 打不开，也可以检查并释放 `5173` 端口：

```powershell
netstat -ano | findstr :5173
Stop-Process -Id <PID> -Force
```

处理后重新启动：

```bash
cd server
go run main.go
```

```bash
cd client
npm run dev
```

## 15.6 Git 提交前检查

提交代码前建议确认 `.gitignore` 已排除以下内容：

```text
node_modules/
client/node_modules/
dist/
client/dist/
*.db
*.db-shm
*.db-wal
*.log
```

如果之前误执行过 `git add .`，可以使用下面命令先取消暂存，再重新按需添加：

```bash
git restore --staged .
git add server client .gitignore "完整项目构建说明.md" "接口约束与质量检测说明.md"
```

注意：`git restore --staged .` 只取消暂存区，不会删除本地文件内容。

---

# 十六、环境配置建议

## 16.1 后端配置文件

后端配置文件位于：

```text
server/config/config.yaml
```

当前项目默认使用 SQLite，适合本地演示和答辩环境，不需要额外安装数据库服务。配置示例：

```yaml
server:
  port: 8080

mysql:
  host: 127.0.0.1
  port: 3306
  username: root
  password: 123456
  database: campus_second_hand
  charset: utf8mb4
  driver: sqlite
  path: campus_second_hand.db

jwt:
  secret: campus_second_hand_secret
  expire_hours: 24
```

说明：

1. `server.port` 控制后端端口，默认 `8080`。
2. `mysql.driver` 为 `sqlite` 时，数据保存在 `server/campus_second_hand.db`。
3. 如需使用 MySQL，可将 `mysql.driver` 改为 `mysql`，并配置 host、port、username、password、database。
4. `jwt.secret` 用于 Token 签名，正式部署时应替换为更复杂的密钥。

## 16.2 MySQL 建库语句

如果切换为 MySQL，需要先创建数据库：

```sql
CREATE DATABASE campus_second_hand DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

## 16.3 质量检测与响应时间检查

答辩或提交前建议完成以下检查：

```bash
cd server
go mod tidy
go build ./...
```

```bash
cd client
npm run build
```

接口可用性检查：

```bash
curl http://localhost:8080/api/ping
```

系统质量说明：

1. 后端统一 JSON 响应，成功和失败都有明确 `code`、`message`、`data`。
2. 后端响应头包含 `X-Response-Time`，可用于观察接口响应耗时。
3. 后端请求日志记录请求方法、路径、状态码和耗时，便于定位慢接口。
4. 详细接口约束、权限校验、质量检测记录见 `接口约束与质量检测说明.md`。

---

# 十七、Codex 需要完成的具体任务

请根据本文档生成完整项目代码，要求尽量做到可运行、结构清晰、适合课程作业演示。

## 17.1 后端任务

1. 搭建 Go + Gin + GORM 项目结构。
2. 完成 MySQL 数据库连接。
3. 实现数据库 AutoMigrate。
4. 创建 User、Product、Transaction、Message、Review 模型。
5. 实现用户注册、登录、个人信息查询和修改。
6. 实现 JWT 生成和鉴权中间件。
7. 实现商品发布、修改、列表查询、详情查询、我的商品、下架商品。
8. 实现交易申请、接受、拒绝、完成、买家交易记录、卖家交易申请。
9. 实现留言发布、回复、查看、删除。
10. 实现评价发布、查看、修改。
11. 实现统一响应结构。
12. 实现基础参数校验和权限校验。
13. 处理 CORS，保证前端可以访问后端接口。
14. 提供 main.go，确保后端可以直接运行。
15. 提供基础测试数据初始化逻辑或说明。

## 17.2 前端任务

1. 搭建 React + Vite + Ant Design 项目结构。
2. 配置 react-router-dom 路由。
3. 封装 axios 请求工具。
4. 实现 Token 保存、读取和清除。
5. 实现登录页和注册页。
6. 实现商品列表页。
7. 实现商品详情页。
8. 实现发布商品页。
9. 实现编辑商品页。
10. 实现我的商品页。
11. 实现我发起的交易页。
12. 实现我收到的交易申请页。
13. 实现个人信息页。
14. 实现交易评价页。
15. 在商品详情页集成留言列表、发表留言、回复留言、删除留言功能。
16. 在商品详情页集成评价列表展示。
17. 使用 Ant Design 组件优化页面效果。
18. 前端页面需要有基础导航栏，方便演示所有功能。

---

# 十八、Git 提交规范

建议按功能阶段提交：

```bash
git add .
git commit -m "feat: 初始化校园二手商品交易系统项目结构"

git add .
git commit -m "feat: 实现用户注册登录和JWT鉴权"

git add .
git commit -m "feat: 实现用户信息管理功能"

git add .
git commit -m "feat: 实现商品发布和商品修改功能"

git add .
git commit -m "feat: 实现商品列表和商品详情功能"

git add .
git commit -m "feat: 实现交易申请和交易处理功能"

git add .
git commit -m "feat: 实现交易记录查询和完成交易功能"

git add .
git commit -m "feat: 实现留言发布回复查询删除功能"

git add .
git commit -m "feat: 实现交易评价发布查询修改功能"

git add .
git commit -m "fix: 完善权限校验和异常处理"

git add .
git commit -m "docs: 补充项目运行说明和接口文档"
```

---

# 十九、验收标准

项目完成后应满足以下要求：

1. 后端项目可以正常启动。
2. 前端项目可以正常启动。
3. 前端可以正常调用后端接口。
4. 用户可以注册。
5. 用户可以登录。
6. 登录后可以查看和修改个人信息。
7. 登录后可以发布商品。
8. 用户可以查看商品列表。
9. 用户可以查看商品详情。
10. 商品发布者可以修改自己的商品。
11. 买家可以申请交易。
12. 卖家可以接受或拒绝交易申请。
13. 交易双方可以完成交易。
14. 用户可以查看自己的交易记录。
15. 用户可以在商品详情页发表留言。
16. 用户可以回复留言。
17. 用户可以查看商品留言。
18. 用户只能删除自己的留言。
19. 交易完成后，交易相关用户可以发布评价。
20. 用户可以查看商品评价。
21. 用户可以修改自己发布的评价。
22. 非本人不能修改或删除他人数据。
23. 非交易相关用户不能发布交易评价。
24. 接口返回格式统一。
25. 页面布局清晰，适合作为课程作业展示。
26. 代码结构清晰，方便后续写实验报告和录制演示视频。

---

# 二十、README 要求

请在项目根目录生成 README.md，内容至少包括：

1. 项目名称
2. 项目简介
3. 技术栈
4. 项目结构
5. 数据库配置说明
6. 后端运行方式
7. 前端运行方式
8. 核心功能说明
9. 接口说明
10. 测试账号或初始化数据说明
11. 常见问题说明

---

# 二十一、代码生成注意事项

1. 优先生成可运行的最小完整版本，不要过度设计。
2. 后端接口、数据库模型和前端页面必须对应起来。
3. 代码中适当添加中文注释，方便后续写实验报告。
4. 不要只生成单个模块，要生成完整项目。
5. 如果某些高级功能实现复杂，可以先用简单方式实现，例如图片上传可以先用图片 URL 字段代替。
6. 前端页面不需要过度美化，但要清晰、可操作、适合录屏演示。
7. 后端要有清晰的错误提示，方便 Postman 测试。
8. 前端要处理未登录跳转、请求失败提示和操作成功提示。
9. 请保持 Go 代码、React 代码结构清晰，便于后续修改。
10. 请尽量保证生成后执行 `go run main.go` 和 `npm run dev` 可以直接启动。
